steps:
  - label: info
    command: 
      - node --version
      - yarn --version
      - bazel info
    plugins:
      - docker#v2.0.0:
          image: "angular/ngcontainer:0.7.0"
          workdir: /src