steps:
  - label: info
    command: 
      - node --version
      - yarn --version
      - bazel info
    plugins:
      - docker#v2.0.0:
          image: "microsoft/dotnet:latest"
          workdir: /src